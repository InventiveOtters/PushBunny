version: '3.8'

services:
  backend:
    build: .
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=postgresql://pushbunny:pushbunny@db:5432/pushbunny
      - N8N_URL=https://inotter.app.n8n.cloud/webhook/a341803a-34a6-48f5-95cc-a74fb35b6518
      - API_KEY_SECRET=dev-secret-key
      - DEBUG=true
    depends_on:
      - db
    volumes:
      - ./app:/app/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8080 --reload

  db:
    image: postgres:15-alpine
    environment:
      - POSTGRES_USER=pushbunny
      - POSTGRES_PASSWORD=pushbunny
      - POSTGRES_DB=pushbunny
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
